---
name: artisan-paper-deck
description: >-
  Use this skill whenever designing, authoring, coding, or presenting 16:9 slide decks,
  keynotes, pitch decks, or presentations with either physical artisan paper aesthetics
  (tactile 400GSM cotton stationery, 9 master paper palettes with intelligent contrast inversion)
  OR quiet luxury minimal editorial layouts (single warm neutral ground, 3-color discipline,
  0.5-1px hairline geometry, four voices typography, 55%+ air metrics).
  Enforces authoritative presentation doctrines (Minto's Pyramid & Action Titles, Duarte's 3-second glance rule,
  Reynolds' Zen restraint, CRAP principles, Tufte's data-ink ratio) and pure doctrine-driven organic spatial composition
  (form follows content, zero formulaic templates). Powered by a unified Deck interaction standard:
  fixed 16:9 JS transform stage scaling, fullscreen mode (F), GSAP reveals (R), automated full-coverage text editing
  with localStorage persistence (E & Cmd+S), and seamless 16:9 PDF export (P).
---

# 双风格演示文稿规范：Artisan Paper & Minimal Editorial

本技能用于规范 **16:9 网页级演示文稿、演讲胶片与 Pitch Deck** 的生成与设计。  
避免传统 PPT 中大段文字堆砌、高光渐变与无序色彩，支持两种视觉风格，并内置清晰的图示化思维与 16:9 交互支持：

1. **风格 A：Artisan Paper（手工纸质风）**：融入纸张质感、活页打孔、票券撕边等实体物料细节，支持自适应墨色对比；
2. **风格 B：Minimal Editorial（极简编辑风）**：类似杂志与画册排版，暖中性底色、细线几何、克制的三支配色与大面积留白。

文稿均遵循“观点先行、图示主导、信息精简、克制排版”的原则，并共享统一的底层交互逻辑。

> [!IMPORTANT]
> **零摩擦单风格直出守恒律 (Zero-Friction Single-Style Direct Delivery)**：
> 每次执行任务，**仅且必须生成一种选定的风格**！严禁同一套 PPT 混用两套风格，更**严禁一次性堆砌输出两套不同风格的代码**。
> - **用户未明确指定风格时**：**严禁中断流程反问用户**！智能体必须根据用户提供的文案主题、语调与受众，**自主研判并决断最合适的一种风格直接生成交付**；
> - **用户明确指定风格时**：严格遵照用户的指示执行。


---

## 0. 统一 Deck 交互引擎守恒律 (Core Deck Interaction Engine)

为确保演示文稿在任何终端（超宽带鱼屏、13 寸笔记本、平板、投影仪）均能展现绝对一致的像素级构图，**两大风格共享同一套高稳定性底层交互引擎**：

### 0.1 16:9 固定舞台矩阵（Fixed 16:9 JS Transform Stage）
1. **原生 1280×720 基准画布**：
   - 所有页面内容在内部以 `1280px × 720px`（或 `1920px × 1080px`）的固定坐标空间进行设计排版。
2. **JavaScript 动态等比居中缩放**：
   - 外部视口容器（`#deck-viewport-wrap`）占满可用窗口；
   - 通过 JS 监听窗口尺寸变化，动态计算：
     ```js
     const scale = Math.min(viewportWidth / 1280, viewportHeight / 720);
     deckStage.style.transform = `translate(-50%, -50%) scale(${scale})`;
     ```
   - 无论窗口如何缩放，幻灯片永远保持 16:9 比例绝对居中（Letterbox / Pillarbox），**绝不出现横向或纵向滚动条，内容永不溢出撑破**。
3. **幻灯片切换防冲突机制**：
   - 严禁直接使用 `display: none` 与 `display: block` 破坏卡片内部原有的 `display: flex/grid`；
   - 统一采用 `.slide-pane`，非激活页面使用 `visibility: hidden; opacity: 0; pointer-events: none;`，激活页面使用 `visibility: visible; opacity: 1; pointer-events: auto;`。

### 0.2 键盘、手势与边缘翻页系统
- **键盘导航**：按 `←` / `↑` / `PageUp` 上一页；按 `→` / `↓` / `Space` / `Enter` / `PageDown` 下一页；按 `Home` 首屏，按 `End` 末屏；
- **手势滑动**：支持移动端或触控板 `touchstart` / `touchend` 水平滑动切换；
- **边缘点击**：桌面端左右悬浮翻页按钮，点击左右两侧即可翻页；
- **Hash 锚点联动**：支持 URL `#1`, `#2` 直接定位指定页面并同步刷新；
- **顶栏快捷键提示自适应 (Responsive Shortcut Hints)**：
  - 顶栏快捷键提示严禁写死 `hidden xl:inline`，以防小屏或笔记本非全屏时直接被隐藏；
  - 必须采用响应式阶梯呈现（大屏显示全称，中小屏精简显示 `[←/→] 翻页 · [F] 全屏 · [E] 编辑 · [P] 打印`，超小屏保留 `[←/→] [F] [E]`），确保任何视口宽度下均一目了然。

### 0.3 全屏放映模式（按 `F` 键）
- 按键盘 **`F`** 键进入沉浸式演讲全屏：
  - 自动隐退顶栏导航、外置翻页按钮与配置抽屉；
  - 视口背景色无缝延展当前幻灯片的底板色，**杜绝黑边与违和感**；
  - 鼠标静止时自动隐退指示器，点击屏幕左侧 35% 翻上一页，点击右侧 65% 翻下一页；
  - 按 `Esc` 或再按 `F` 退出全屏。

### 0.4 GSAP 逐层阶梯微动效与重播（按 `R` 键）
- 翻页到达新幻灯片时，通过 GSAP 时间线编排细腻的微交错逐层入场动效（标题、细线、卡片、图表、印章等波浪级联）；
- 按 **`R`** 键或点击工具栏「动效重播」随时重新播放当前页入场动画；
- 必须支持 `@media (prefers-reduced-motion: reduce)` 无障碍降级。

### 0.5 全量自动化行内即时编辑与持久化（按 `E` 键 & `Cmd+S`）
> [!IMPORTANT]
> **拒绝人工漏标，底座脚本全自动智能扫描（Auto-Discovery Engine）**：
> 过去依赖人工或 AI 手写 `data-editable="true"`，不可避免会遗漏列表、标签、副标或页脚，导致部分文字无法点击修改。
> **现已彻底改为底层脚本自动化托管**：
> 1. **全自动扫描注册（`autoRegisterEditableElements()`）**：
>    - 页面加载时，脚本自动深度遍历所有 `.slide-pane` 内的文字叶子节点（`h1–h6, p, li, span, div, strong, em, b, i, a` 等）；
>    - 凡是包含有效文字、且不属于系统控件/SVG 的元素，自动注入 `data-editable="true"` 并分配唯一的 `data-edit-id="s{slide}_auto_{index}"`，实现 **100% 文本无死角覆盖**；
> 2. **统一 Class 快速标定支持**：
>    - 同时也原生支持用户或模板显式标记 `.editable-text` 或 `.deck-text` 类名，两套机制无缝并存；
> 3. **极简排除标记**：
>    - 如有固定不允许用户篡改的字段（如版权号），仅需添加 `data-no-edit` 属性，扫描器将自动忽略；
> 4. **行内直编模式（按 `E`）**：
>    - 按 `E` 键或点击「编辑文案」按钮，全屏所有文字统一激活 `contentEditable = true`，并挂载高对比虚线框（`outline: 1.5px dashed var(--ink-stamp)`），点击任意处即可直接打字修改；
>    - **按钮状态与反色悬停保护**：进入编辑态（`body.is-editing-mode`）时，按钮显示「完成编辑」并切换为深底白字（如 `#823838`），**必须为 `:hover` 配置更深色背景（如 `#632626`）与白字**，严禁使用 Tailwind `hover:bg-white` 导致悬停时出现白底白字；
> 5. **持久化与回显（`Cmd+S`）**：
>    - 失去焦点或按 `Cmd+S` 时，全量提取所有修改至 `localStorage`，刷新后永久保留；抽屉内支持一键重置恢复出厂。

### 0.6 印刷级 16:9 无损 PDF 导出（按 `P` 键 / `Cmd+P`）
- 内置高精度 `@media print` 样式（`@page { size: 16in 9in; margin: 0; }`），按 `P` 或 `Cmd+P` 即可调起浏览器无损导出 16:9 矢量画册 PDF；
- 打印触发时，系统自动跳过 GSAP 进行中的补间，将所有元素直接置为完成态（`opacity: 1`），避免截出半透明或空白；
- 配套提供 Puppeteer 批量自动化导出脚本：`node scripts/export_pdf.js input.html output.pdf`。

### 0.7 调配抽屉配置项纯净律 (Pure-Style Drawer Discipline)
> [!IMPORTANT]
> **生成的 HTML 调配抽屉（`#drawer`）严禁包含任何“跨风格换肤”伪切换开关**：
> 1. **两种风格排版骨架本质不同**：
>    - 风格 A（Artisan Paper）依托“双层纸板卡片 + 印章/物料”的 DOM 骨架；
>    - 风格 B（Minimal Editorial）依托“无框大留白 + 出血发丝线 + 四声部”的 DOM 骨架；
>    - 页面排版结构写在 HTML 节点中，无法仅凭前端几行 CSS/JS 在运行时动态重构。前端的“风格切换”只会导致只变颜色不变骨架，严重破坏风格质感与用户感知。
> 2. **抽屉选项纯粹归属当前选定风格**：
>    - **输出【风格 A · Artisan Paper】时**：抽屉标题为「§ 工坊调配面板」，仅包含「9 大工坊纸质母色」调配网格、排印字体切换、文案持久化与导出，**严禁放置切换到 Minimal Editorial 的跨风格按钮**；
>    - **输出【风格 B · Minimal Editorial】时**：抽屉标题为「§ 留白静奢配置」，仅包含「6 大单色暖中性母盘」调配网格、排印字体切换、文案持久化与导出，**严禁放置切换到 Artisan Paper 的跨风格按钮**。
> 3. **换风格必须重新生成代码**：
>    - 若用户在看完效果后希望切换风格，正确路径是在对话端告知智能体，由智能体基于对应风格的构图语法与空间逻辑，重新从文案推导并全量生成新 HTML，坚决杜绝在前端做虚假的“半吊子换肤开关”。

---

## 1. 权威 PPT 理论与排版内容指南 (Authoritative Presentation & Content Doctrine)

一份优秀的演示文稿不是“搬运了 Word 的提词板”，而是**高度提炼的结构化视觉叙事**。制作 PPT 时必须强制贯彻以下经典大师准则：

### 1.1 经典理论的工程化落地规范

| 权威理论源流 | 核心法则 | 在 Deck 排版与文案中的硬性约束 |
| :--- | :--- | :--- |
| **Barbara Minto《金字塔原理》** | **结论先行 (BLUF) & 行动导向标题 (Action Titles)** | **杜绝“名词短语式标题”**（严禁只写“业务概况”、“数据表现”、“竞品分析”）。主标题必须是**包含结论或论断的完整陈述句**（如：“三阶段闭环驱动，高客单转化率跃升 42%”）。听众只看主标题就能把握全页核心逻辑。 |
| **Nancy Duarte《Slide:ology》** | **3 秒看懂法则 (The 3-Second Glance Test)** | 幻灯片属于“瞬间认知媒介”。观众应在 **1.5–3 秒内看清主标题断言与视觉焦点**。凡需观众停顿阅读 5 秒以上的版面，一律强制删词重构。 |
| **Garr Reynolds《演说之禅 (Presentation Zen)》** | **大道至简 (Simplicity) & 留白 (間)** | **“文字越多，力量越弱；文字越少，回响越深”**。幻灯片是视觉的放大器，绝不是演讲者的提词板！彻底消灭说明性长篇累牍，给心智留出呼吸与共鸣的余地。 |
| **Seth Godin《极简演讲律》** | **字数预算硬约束 (Strict Word Budget)** | **单页全版汉字量严格控制在 25–45 字以内（绝对上限 ≤ 60 字）！** 严禁出现超过 2 行或超过 25 字的连续段落。一切解释性叙述交由讲者口述，画面只留最纯粹的论断、数据与锚点。 |
| **Robin Williams《写给大家看的设计书》** | **CRAP 四大排版基石** | - **C 对比 (Contrast)**：字号级差必须悬殊（64–140px 巨字 vs 11–13px 短注）；<br>- **R 重复 (Repetition)**：元信息两角制、发丝线粗细（0.5–1px）全篇统一；<br>- **A 对齐 (Alignment)**：严格网格基线对齐；<br>- **P 亲密性 (Proximity)**：间距即逻辑，大空白划分层级。 |
| **Edward Tufte《量化信息视觉化》** | **最大化数据墨水比 (Data-Ink Ratio)** | 每一个像素、每一个汉字都必须承载最高浓度语义，剔除所有废话垫字与口水话。 |

### 1.2 大道至简：单页文字精炼消减法 (The 3-Step Word Eraser)
生成任何文稿时，必须强制经过三次“文案脱水”：
1. **第一次脱水：删去一切背景铺垫与过渡句**（如删除“在XXX中提出”、“我们通常认为”、“值得注意的是”等废话）；
2. **第二次脱水：将复杂复句压缩为短语**（如将“如果一个切点能够完美传递角色心境与观众情绪的共振，即便空间跳跃观众也不会出戏”压缩为“**宁可轴线崩塌，绝不情感游离**”）；
3. **第三次脱水：数算字数**（全页字数是否 ≤ 45 字？若超标，继续斩断修饰词，直到一览无余）。

### 1.3 幻灯片内容三层级剥离架构 (Triad Information Hierarchy)
每页内容严格剥离为极简的三层：
1. **Layer 1: 论断层（Action Title + Kicker）**：
   - 顶部 10px 全大写 Kicker 标定分类；
   - 36–48px 衬线主标题宣告本页核心断言（10–14 字以内）；
2. **Layer 2: 焦点层（Primary Graphic / Focus Word）**：
   - 占据画面 60%+ 黄金区域，为 1 个巨型数字（120px+）、1 组轻量波形/刻度轨、或极端留白的大字；
3. **Layer 3: 证据注记层（Micro-Annotation）**：
   - 至多 1–2 句微型短注（每句 ≤ 15 字），字号 11–13px，围绕焦点精炼点缀。严禁成段堆砌！

---

## 2. 系统化图示设计体系与有机排版思维 (Visual Thinking & Organic Composition)

> [!IMPORTANT]
> **杜绝模板固化与形式追随内容法则 (Anti-Template & Form Follows Content)**：
> **严禁机械套用任何固定模版！** 演示文稿绝不是让用户在几套死板模版中做单选题。
> 本规范中的所有图示、组件与版式，**全部仅为底层思维原型与启发思路（Design Inspiration & Thought Seeds）**。
> 智能体必须根据用户提供的具体文案、逻辑层级与数据诉求，**现场自由发散、有机推导最合适的排版形态**：
> - 涉及时间与流程？推导细密时间线、里程碑票券、工序阶梯；
> - 涉及因果增强？推导循环飞轮、闭环回路；
> - 涉及定量对比与趋势？自主构建**柱形对比图 (Column Bar Chart)**、数据走势刻度线；
> - 涉及权重与结构比例？自主构建**结构比例圆环图 (Donut Ring Chart)**、同心嵌套圆；
> - 涉及关键定量突破？自主构建**大字号纯数字看板 (Pure Numbers)**、高数据墨水比度量衡；
> - 涉及多维战略权衡？自主构建**十字象限散点、拱顶题名色带、双栏对比**；
> - 涉及实体物料叙事？融入**双联活页装订、锯齿撕边票根、档案火漆印章**。
> **排版永无定式，唯内容与逻辑是瞻！**

### 2.1 核心图示族谱与语义匹配矩阵

```
                       ┌── 线性流程链 (Linear Pipeline)
                       ├── 阶段里程碑票券 (Milestone Roadmap / Ticket)
    1. 演进与序列 ─────┼── 闭环循环飞轮 (Loop Cycle / Flywheel)
    (Evolution/Time)   └── 细密刻度时间线 (Tick Timeline)
                       
                       ┌── 同心嵌套圆 (Nested Circles / Inclusion)
    2. 结构与层级 ─────┼── 侧线支柱导轨 (Side-Rule Pillars / Foundations)
    (Hierarchy/System) ├── 金字塔分层梯形 (Pyramid / Tiered Layers)
                       └── 结构比例圆环图 (Donut Ring Chart / Segmentation)
                       
                       ┌── 十字象限散点 (2×2 Quadrant Matrix)
    3. 对比与决策 ─────┼── 双栏对照对决 (Before vs After / Dual Column)
    (Comparison/Choice)├── 规整色带条列 (Banded Principles List)
                       └── 柱形对比图表 (Column Bar Chart / Benchmark)
                       
                       ┌── 极简纯数字看板 (Pure Numbers & Quantitative Assertion)
    4. 数据与焦点 ─────┼── 巨号陈述看板 (Hero Number Statement)
    (Data & Metrics)   ├── 工业裁切框景 (Crop Mark Frame / Thesis)
                       └── 标本技术矩阵 (Specimen Metric Grid)
```

### 2.2 纯粹矢量发丝几何语言 (Hairline Vector Geometry)
所有图形设计均建立在**高精度的纯平 2D 几何线条**之上，坚决抵制任何塑料感：
1. **统一线宽**：主结构线统一采用 `0.5px` 或 `1px` 发丝细线（Hairline），严禁粗笨黑框；
2. **开放式箭头 (Open Markers)**：
   - 流程连接一律使用精细开放箭头，避免粗大实心三角块：
     ```html
     <marker id="ah" markerWidth="7" markerHeight="7" refX="5" refY="3.5" orient="auto">
       <path d="M1,1 L6,3.5 L1,6" fill="none" stroke="currentColor" stroke-width="1"/>
     </marker>
     ```
3. **几何锚点**：节点采用直径 `6–8px` 的正圆小点，或空心带 `1px` 边框圆点；
4. **单线矢量微标**：图标严格使用 1px 单线描边（如 Lucide / Feather 风格），严禁彩色免扣小图或 Emoji。

### 2.3 图形配色纪律与单色着色法 (Diagrammatic Color Theory)
图形配色必须遵守严苛的**三色上限**与层次分级，杜绝彩虹糖式的混乱着色：
1. **基底骨架（占比 ~70%）**：
   - 图形的框架线条、未激活圆环、坐标轴、连接虚线：统一使用墨色的微透明变体：`rgba(ink, 0.25 ~ 0.4)`；
2. **面域底色（占比 ~20%）**：
   - 若图形内需划分区域（如色带、同心圆底面、徽标底色）：仅允许使用 4%–8% 极淡墨色或底板同色，严禁高饱和彩色大面积铺底；
3. **单一强调色锚点（占比 < 8%）**：
   - **全图至多允许 1–2 处高亮锚点**！
   - 强调色（如陶土红 `#A65235`、橄榄绿 `#4C4A2E`、暖褐 `#7A5C43`、灰金 `#9C8A6B`）仅用于标注：**流程当前阶段、象限最优解、循环启动枢纽、增长关键拐点**；
   - 其余节点一律保持中性墨色，形成绝对的视觉焦点。
4. **深底智能反转规则**：
   - 当底板为深色（如 `taupe`, `laid`, `terracotta` 等）时，图形发丝线整体反转为米白高透（`rgba(247,243,236, 0.35)`），高亮锚点采用高明度提亮色，杜绝深底暗线。

### 2.4 图形空间工程学与安全呼吸锁 (Diagram Airflow & Safety Rules)
1. **舞台中轴绝对对齐**：
   - 闭环圆、同心圆、十字象限、横向时间线等中心对称型图表，图表中心坐标必须严格锚定在画布中轴 `X = 640px` 上，绝不左右漂移；
2. **防压线与防穿插距离**：
   - SVG 连线、圆弧、刻度指针与文字标签之间**必须预留 ≥ 25px 安全距离**，严禁连线直接压在文字上方或切割文字；
3. **文字防折行锁**：
   - 图形内的所有步骤名称、坐标轴指标、度量单位一律显式声明 `white-space: nowrap;`，防止因为视口缩放发生丑陋的单字折行；
4. **正文排版防孤字**：
   - 段落末尾严禁单字成行（孤字）或单个标点独占一行。

---

## 3. 双风格对比与智能自适应匹配决策 (Two Visual Systems & Autonomous Selection)

AI 智能体在处理演示文稿时，**严格执行“单次交付单一风格”**。依据下表与用户输入的内容语义，进行**零摩擦自主决策**：
- **用户未明确指定风格时**：根据内容定位与场景属性自主选择，直接生成完整成品，**严禁停下反问用户**；
- **用户明确指定风格时**：严格按照用户指定风格执行。

| 维度 | 风格 A：Artisan Paper (手工物料典藏) | 风格 B：Minimal Editorial (留白辑要 · 静奢极简) |
| :--- | :--- | :--- |
| **设计隐喻** | 物理纸张、欧洲活页装订、手作票券、印章工坊 | 高端品牌画册、日系/欧美独立杂志、画廊展签 |
| **视觉基底** | 双层结构：底板纸板 + 悬浮棉纸卡片，微距纤维噪点 (`#paper-fiber-overlay`) | 单层纯平：统一暖中性底色，极端平坦，零纸纹，零投影 |
| **线条与结构** | 物理内凹打孔透光、锯齿撕边、印章虚线框 | 0.5–1px 墨色发丝细线、细线开放箭头、十字坐标、出血裁切 |
| **装饰元素** | 火漆压印红印章、工坊标本徽记、档案编号 | 极简老式衬线数字、斜体手写批注、两角制元信息框架 |
| **色彩纪律** | 9 大工坊纸色系统，支持深底基色智能反转文字 | 单色纪律三色上限：底色 + 墨色 + 至多 1 个低饱和强调色 (<8%) |
| **排版密度** | 物料厚重、沉稳、结构层次鲜明 | 空气感极强、大面积留白、焦点明确、不对称呼吸 |
| **最佳适用场景** | 融资路演 Pitch Deck、品牌发布会、年度汇报、工艺匠心演讲、教育培训 | 静奢商业提案、策略咨询思考、设计作品集、摄影/文学分享、思考笔记 |

---

### 3.1 风格 A 规范：手工物料与纸质典藏系统 (Artisan Paper System)

#### 物理纸质触感与去数码化原则
1. **严禁数码浮夸投影**：一律杜绝常规大面积模糊软阴影，保持纯平物理纸张质感；
2. **微距纸纤维漫反射层（Fiber Noise Overlay）**：
   - 舞台常驻微距纸纤维纹理，`mix-blend-mode: multiply`，呈现真实 400GSM 棉纸的漫反射哑光触感；
3. **触感印墨与发丝压痕（Tactile Inks & Debossed Hairlines）**：
   - 分割与连接均采用 0.5–1px 压印质感细线，色彩取自矿物印泥或古籍墨色；
4. **彻底杜绝套路化具象排版参考**：
   - **全面移除“打孔活页”、“撕边票据”、“火漆印章”、“居中文献卡”等一切具象排版参考**！
   - 严禁机械拼凑具象物件。高级感来自纯粹的字阶级差（Type Scale）、从容舒展的留白空间（Air Ratio > 55%）与沉静的纸面矿物色调。

#### 9 大工坊纸质母色与智能文字对比反转系统

| 色谱标识 | 中文名称 | 底板基色 (Board) | 悬浮卡纸 (Card) | 底板主文字 (Text) | 印章与高亮标注色 (`--ink-stamp`) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `celadon` | **冷杉青绿** (默认) | `#9EABA2` | `#F7F4EC` | 深墨灰 `#1E2826` | 火漆深红 `#823838` (高反差) |
| `terracotta` | **陶土红棕** | `#9B4F3B` | `#F6F3EC` | **🌟 自动反转为纯净柔和米白 `#F7F3EC`** | **🌟 智能反转为琥珀暖金 `#F2BD64` (严禁红底红字，杜绝隐形)** |
| `butter` | **黄油票据** | `#E5B84B` | `#FFFDF7` | **🌟 保持浓郁深炭黑字 `#221A0C`** | 浓郁赤红 `#8A2D1F` |
| `olive` | **橄榄麻布** | `#686034` | `#F5F4EE` | **🌟 自动反转为典雅象牙白 `#FAF8F0`** | **🌟 智能反转为麦穗草木金 `#E8BF5C`** |
| `blush` | **干枯粉彩** | `#C7A296` | `#FDFBF9` | 深褐炭黑 `#2A1D1B` | 绛红印泥 `#823838` |
| `beige` | **燕麦米色** | `#D8D2C4` | `#FAF8F4` | 浓郁墨黑 `#27231E` | 锈红印墨 `#8A3B2C` |
| `pistachio` | **薄荷浅绿** | `#D6E0B9` | `#FCFDF7` | 墨绿深黑 `#1D2A1C` | 赭石朱砂 `#7A3225` |
| `oyster` | **暖灰水泥** | `#CECBC5` | `#FCFBFA` | 深沉岩炭黑 `#242321` | 暗红印章 `#7E3636` |
| `laid` | **古典棉纸** | `#4A4139` | `#F7F5EE` | **🌟 自动反转为典籍金米白 `#F8F5EE`** | **🌟 智能反转为典籍烫金 `#E5AF65`** |

---

### 3.2 风格 B 规范：留白辑要 · 静奢极简编辑系统 (Minimal Editorial System)

#### 核心视觉哲学：让画面安静下来，让文字自己成为图形
1. **一画一焦点**：每页只有一个主要视觉焦点，绝不允许两个并列重量争抢视线；
2. **细线代替色块**：全部分隔、框定、连接全部使用 `0.5–1px` 发丝线（hairline），与墨色同色、`50–70%` 不透明度，严禁灰色色块分区与阴影；
3. **数字即装饰**：`01. / 02. / 03.` 老式衬线数字本身就是装饰，放大至与标题同级或更大；
4. **越界制造张力**：发丝细线或几何弧线允许**出血**（延伸冲出画布边缘被裁切）；
5. **不对称呼吸**：上下留白刻意不均，焦点偏离几何中心 3–8%，杜绝死板填满。

#### 单色纪律 (One-Neutral Discipline)
- **底色系（一次只选一套，全篇统一）**：
  - 象牙灰 `ivory`（`#F2F0EB`，墨色 `#1E1C1A`）；
  - 暖米灰 `greige`（`#E5E1D8`，墨色 `#26231F`）；
  - 亚麻奶油 `cream`（`#F5F1E6`，墨色 `#221F1A`）；
  - 深陶土灰褐 `taupe`（`#8A8177`，墨色反转为米白 `#F5F2EA`）；
  - 橄榄米 `olive-beige`（`#DDD5C4`，墨色 `#2A261C`）；
  - 燕麦白 `oat`（`#EDEAE3`，墨色 `#24211D`）。
- **三色上限**：底色 + 墨色 + 至多一个低饱和强调色（陶土红 `#A65235`、橄榄绿 `#4C4A2E`、暖褐 `#7A5C43`、灰金 `#9C8A6B`），强调色总覆盖面积 `< 8%`。

#### 四声部字体系统 (Four Voices)
一张画布上严格只出现四种字态：
1. **① 独唱 `display`**：高对比衬线（Playfair Display / Noto Serif SC），用于巨词、主标题、大数字，40–120px；
2. **② 低语 `kicker`**：同衬线或无衬线全大写，用于页眉品牌、页脚页码、分类标签，10–13px，字距 `0.25–0.35em`，透明度 60–80%；
3. **③ 叙述 `body`**：几何无衬线细体（Jost / Plus Jakarta Sans）或等宽（Courier Prime），14–18px，行高 1.8–2.0，单块正文不超过 6 行；
4. **④ 耳语 `annotation`**：衬线斜体或手写体，用于引语、批注、步骤副标题，以强调色渲染。

#### 元信息框架：两角制 (Two-Corner Meta Frame)
- **左上角**：本页描述语（`kicker` 声部，如 `ARCHITECTURE / DESIGN SYSTEM`）；
- **右上角**：`章节名 · 页码`（如 `SECTION · 02`）；
- **底部默认留空**：多页文稿底部不堆砌无意义信息，保持极度干净。

---

## 4. 排版反套路律与自由扩散创作法 (Anti-Formulaic Doctrine & Organic Layout Composition)

> [!CAUTION]
> **严禁机械套用固定模板（Anti-Cliché Rule）**：
> 本技能提供的图示版式（闭环、同心圆、票据、活页打孔、标本网格等）是**可自由拆解组合的“设计原子与灵感语汇”（Design Primitives & Atoms），绝非机械填充的“整页固定模版”**！
> 严禁每次制作 PPT 都无脑复用“打孔活页 + 撕边票据 + 独立物料卡 + 标本对照表”老几样。用户需要的是针对其特定内容的个性化高级排版，而非千篇一律的八股文。

### 4.1 内容原发生长法则 (Content-First Composition)
版式结构必须**由用户具体内容的内在逻辑自然推导生成**，而非先找一个空壳模板再把字塞进去：
1. **纯字排版即最高级图形（Typography as Visual Architecture）**：
   - 深刻的观点不需要强行画图！当文案具有强烈哲思或震撼力时，采用 **80–120px 巨号衬线主词 + 微距两角制元信息 + 1px 单侧发丝线 + 宽松行距**，留白本身就是最昂贵的视觉语言；
2. **无框化流动排版（Frameless Flow）**：
   - 摒弃到处套用“白色悬浮卡片”的惯性思维！直接在底板上借助间距层次（Proximity）、微型锚点标尺（`· 01 // PRINCIPLE`）与穿插的发丝分割线组织内容，让版面轻盈通透；
3. **彻底去模板化（Zero-Template Doctrine）**：
   - **本 Skill 绝不包含任何固定排版参考模板**。所有的布局必须根据当页内容的语义关系（对立、递进、脉冲、总括）从零推演；
   - 彻底告别“每做一次 PPT 就把旧排版抄一遍”的思维定式，文字与留白即是终极架构。

### 4.2 版式无限扩散的五大构图维度 (5 Dimensions of Layout Expansion)
制作每一页时，从以下五个维度自由探索无限多样的版面：
1. **不对称极化切分（Radical Asymmetric Split）**：
   - 2:8 黄金切分（左侧极窄导引轨，右侧极宽呼吸展面）；
   - 上下三段错层（顶栏论断，中段单行巨词，底部横向三列微注）；
   - 悬空偏心焦点（视觉重心故意向左上或右下偏移 5–8%，制造现代美术馆般的画册张力）。
2. **动势与引力场（Dynamic Vectors & Tension）**：
   - 单斜线或水平发丝轴导轨，引导眼球视线流转；
   - 辩证对立的左右场域（双向张力，无需实体卡片，仅靠文字位置与对齐方式呈现对决）；
   - 向心聚拢或离心发散。
3. **微型几何与参数标尺（Micro-Geometry & Tickers）**：
   - 像精密仪器刻度盘一样的微型步进标尺（Tick marks, Crosshairs `+`）；
   - 极简单行状态栏（Status Bar）、代码坐标戳记（Coordinate stamps）。
4. **全篇呼吸节律交响（Macro Pacing & Rhythm）**：
   - 整套 PPT 必须有张有弛：
     - *开场（松）*：极简空镜巨词，全屏呼吸；
     - *框架（中）*：清晰利落的演进或分支架构；
     - *爆点（紧）*：巨号陈述或高对比辩论；
     - *哲思/转折（极松）*：仅一两句至简真理，巨大留白给观众思考时间；
     - *论证（中）*：细密标尺或精细三列导轨；
     - *尾声（松）*：极简巨字无框终章，留白凝视，余音绕梁。
   - 绝不能页页密度相同、页页形式雷同。

### 4.3 纯理论驱动的原发构图语法 (Pure Doctrine-Driven Spatial Grammar)

> [!IMPORTANT]
> **彻底废黜固定模板，每一页都是针对语义的独创构图**：
> 绝不可在脑海中预存任何“票据模版”、“活页模版”或“表格模版”并进行机械填空！
> 智能体必须像专业艺术总监一样，深入阅读本页文案的内在心理学拓扑，运用以下纯粹的空间几何语法，现场生成独一无二的版面：

#### 1. 语义拓扑到空间构图的自然推导 (Form Follows Content)
- **单点顿悟 / 核心金句 (Singular Axiom)**：
  - *做法*：**消灭一切图表与卡片！** 采用 80–140px 巨型字排印核心数字或关键词，四周留出 65% 以上纯粹空气，仅配以微距打字机出处标签与单侧发丝引线。留白本身就是最权威的力量。
- **辩证矛盾 / 对立张力 (Polar Dialectic & Tension)**：
  - *做法*：**无边框两极对决场（如 6:4 或 5:5 对峙）**。左极与右极直接呼吸在底板上，仅靠字号反差、对齐方向与单根垂直发丝中轴划分阵营，呈现剑拔弩张的思考对抗。
- **时间演进 / 律动脉冲 (Temporal Cadence & Waveforms)**：
  - *做法*：**轻盈的贯穿式横向轴轨**。使用 1px 细线、SVG 神经脉冲波形或微型刻度短线（Tick Marks），将节点像音符一样悬挂在轴线上，坚决不套用笨拙的票根。
- **层级序列 / 不可逆取舍 (Cascading Strata & Priority)**：
  - *做法*：**阶梯断层式下沉（Descending Strata）或向心包孕**。通过不等宽的悬挑色带、阶梯收缩或同心发丝圆，让观众第一眼直观感受到“自底向顶的不可逆权重”。
- **多维剖析 / 要素分解 (Multi-Factor Taxonomy)**：
  - *做法*：**轻量侧栏三列或不对称发丝导轨（Side-Rules）**。去除所有围合卡片，直接在底板上纯靠间距对齐（Proximity）与垂直细分割线组织，信息透气通脱。
- **极简终章 / 意犹未尽 (Unframed Epilogue & Semantic Resonance)**：
  - *做法*：**彻底废除任何“居中框盒”、“卡片容器”或“假印章”！** 终章必须是彻底开放的无框化排版。以 50–70px 极具张力的主旨大字横贯版面，右侧或下方辅以一行极细发丝线与出处年份签名，四周留出 65% 以上浩瀚空气，让观点在纯净的纸面自然共振，气象万千。

#### 2. 空间构图的三大永恒准则
1. **尺度悬殊律 (Radical Scale Disparity)**：主视觉字号（64–140px）与说明字号（11–14px）必须拉开 5–10 倍的悬殊差距，杜绝中等字号平庸混杂；
2. **发丝纪律 (Hairline Discipline)**：所有连接与分割统一为 `0.5–1px`、透明度 `0.15–0.25` 的极细发丝线，坚决消灭粗黑重框与塑料投影；
3. **空气留白率 (Negative Space > 55%)**：一页之内至少一半区域为纯粹未被占用的底板呼吸场域，杜绝死板填满。

---

## 5. 智能体端到端执行管线 (Agent End-to-End Workflow)

AI 智能体在接收到 PPT 需求时，执行严谨的五步闭环：

### Phase 0: 需求诊断与风格自动决断 (Autonomous Style Selection & Alignment)
1. **单一套系交付硬约束 (Strict Single-Style Output)**：
   - 绝不可将两套风格混杂在同一篇文稿中，也绝不可在单次回复中一次性堆出两个版本的代码。一次任务只生成并交付一套选定风格的完整 16:9 Deck。
2. **风格决断逻辑 (Zero-Friction Decision Tree)**：
   - **用户明确要求风格**：严格按用户指定执行（如用户提到“极简”、“杂志风”选 B；提到“纸质”、“物料感”、“牛皮纸”选 A）；
   - **用户未指定风格（最常见情形）**：**严禁停下向用户提问**！智能体自动研判内容属性并直接选用：
     - *自动匹配风格 A · Artisan Paper*：融资路演 Pitch Deck、品牌发布会、年度总结汇报、工艺/手作/文化宣讲、活动开幕、实体行业汇报；
     - *自动匹配风格 B · Minimal Editorial*：商业策略咨询、极简思考笔记、设计/摄影作品集、前沿科技洞察、文学哲学思考、个人分享；
   - **交付说明标准**：生成完毕后，在回复末尾附带一句轻量说明（如：“*已根据您的内容主题自动匹配最契合的【风格 X】；如需体验【风格 Y】，随时告诉我即可一键重构。*”）。
3. **明确受众与信息密度**：
   - 现场演讲/路演？采用**演讲型（低密度、图示主导、大字号）**；
   - 报审传阅/技术沉淀？采用**阅读型（中高密度、侧线多列、参数标本）**。

### Phase 1: 内容提炼与金字塔骨架 (Content Structuring)
1. **文案提炼守恒律**：绝不照抄用户大段讲稿，也绝不删漏关键事实；
2. **强制生成 Action Titles（观点标题）**：
   - 逐页检查标题：必须是带有结论的断言陈述句，严禁出现“关于XX”、“市场概况”等无观点名词；
3. **构建三层信息架构**：每页明确 Layer 1 (观点)、Layer 2 (图示)、Layer 3 (论据支撑)。

### Phase 2: 内容驱动的有机版式推演 (Organic Layout Deduction)
> [!IMPORTANT]
> **拒绝死板映射表，因题制宜自由构图**：
> 绝不可看到原则就套票据，看到对比就套活页！智能体必须审视本页核心语义：
> - 这是一个冲突性命题？→ 尝试无框左右极化对抗，或斜向贯穿轴；
> - 这是一个单点深刻洞见？→ 尝试 100px 巨字纯排版与超大留白，消灭多余图形；
> - 这是一个多维分类？→ 尝试错落非对称网格、微标尺导轨，或纯文字层级索引；
> - 全面抛弃一切套路化排版模板（无打孔、无撕边票据、无居中印章卡），100% 依赖内容本身的逻辑推演，靠精密网格、发丝细线、字阶级差与开阔留白塑造高级感。

### Phase 3: 图形配色与代码工程交付 (Implementation)
1. 单文件 HTML 架构，引入 Tailwind CDN + GSAP CDN + Google Fonts（Noto Serif SC, Playfair Display, Jost, Courier Prime）；
2. 注入 16:9 固定舞台 JS 矩阵缩放代码；
3. 注入键盘监听（`←` `→` 翻页、`F` 全屏、`R` 重播、`E` 即时编辑、`P` 打印导出）；
4. 应用**图形配色纪律**：基底发丝线高透明（`rgba(ink, 0.35)`），全图仅 1 处强调色锚点，杜绝彩虹杂色；
5. **抽屉单风格配置纯净性**：配置抽屉（`#drawer`）严格只渲染当前风格所属的母色（Artisan 对应 9 色，Editorial 对应 6 色），**严禁注入跨风格切换开关**。

### Phase 4: 完工自检核对清单 (Pre-Flight Quality Checklist)
在交付前逐项自查：
1. [ ] **Action Titles 审查**：所有页面标题是否均为完整观点断言句？（无死板名词短语）
2. [ ] **3 秒认知测试 (Glance Test)**：每页焦点是否唯一？眯眼观察能否第一眼捕捉论点与图示？
3. [ ] **CRAP 对比与对齐**：大字（44px+）与细字（14px）对比是否明显？网格基线是否绝对平直？
4. [ ] **图示信噪比 (Data-Ink)**：是否清除了所有无意义色块？图形是否为精细发丝线（0.5–1px）？
5. [ ] **强调色克制**：图形高亮节点是否 `<= 2 处`？全页强调色面积是否 `< 8%`？
6. [ ] **空间呼吸与防遮挡锁**：图表中心是否锁定 `X=640px`？文字与连线是否预留 `≥ 25px`？短标签是否加了 `white-space: nowrap`？有无段末单字孤行？
7. [ ] **Fixed 16:9 与全屏放映**：任意浏览器窗口比例缩放无滚动条，按 `F` 全屏无黑边；
8. [ ] **行内编辑与打印**：按 `E` 任意文本可改，按 `Cmd+S` 能保存，按 `P` 无损矢量导出；
9. [ ] **抽屉纯净审查**：调配抽屉中是否只有当前风格的专属母色与设置？绝对杜绝任何跨风格切换按钮。
